#!/usr/bin/env escript
%% -*- erlang -*-
%%! -pa ebin

main(_) ->
    ok = application:start(pubsubpoll),
    {ok, Chan} = pubsubpoll:create_channel({}),
    io:format("Chan: ~p~n~p~n", [Chan, pubsubpoll:channels()]).
